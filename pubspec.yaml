name: finance_app
description: "Personal Finance App - Pencatatan Pengeluaran & Pemasukan Harian"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  path: ^1.9.0
  fl_chart: ^0.69.0
  intl: ^0.19.0
  flutter_platform_widgets: ^6.0.1
  pocketbase: ^0.21.0
  shared_preferences: ^2.3.2
  connectivity_plus: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true